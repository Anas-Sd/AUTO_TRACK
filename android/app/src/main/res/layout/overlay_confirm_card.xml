<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom"
    android:layout_margin="16dp"
    android:background="@drawable/bg_card_border"
    android:elevation="16dp"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Header & Source App Badge -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Payment Detected"
            android:textColor="@color/accent_mint"
            android:textSize="12sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvOverlaySource"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_card_border"
            android:paddingStart="8dp"
            android:paddingTop="2dp"
            android:paddingEnd="8dp"
            android:paddingBottom="2dp"
            android:text="Google Pay"
            android:textColor="@color/text_muted"
            android:textSize="10sp" />
    </LinearLayout>

    <!-- Amount and Type Toggle in horizontal line -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:text="₹"
            android:textColor="@color/text_white"
            android:textSize="22sp"
            android:textStyle="bold" />

        <EditText
            android:id="@+id/etOverlayAmount"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="4dp"
            android:layout_weight="1"
            android:background="@android:color/transparent"
            android:fontFamily="monospace"
            android:hint="0.00"
            android:inputType="numberDecimal"
            android:textColor="@color/text_white"
            android:textColorHint="@color/text_muted"
            android:textSize="22sp"
            android:textStyle="bold" />

        <!-- Income / Expense Toggle buttons -->
        <RadioGroup
            android:id="@+id/rgOverlayType"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:background="@drawable/bg_card_border"
            android:orientation="horizontal"
            android:padding="2dp">

            <RadioButton
                android:id="@+id/rbOverlayExpense"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:background="@android:color/transparent"
                android:button="@null"
                android:checked="true"
                android:paddingStart="10dp"
                android:paddingEnd="10dp"
                android:text="Expense"
                android:textColor="@color/expense_red"
                android:textSize="11sp"
                android:textStyle="bold" />

            <RadioButton
                android:id="@+id/rbOverlayIncome"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:background="@android:color/transparent"
                android:button="@null"
                android:paddingStart="10dp"
                android:paddingEnd="10dp"
                android:text="Income"
                android:textColor="@color/accent_mint"
                android:textSize="11sp"
                android:textStyle="bold" />
        </RadioGroup>
    </LinearLayout>

    <!-- Vendor / Receiver -->
    <EditText
        android:id="@+id/etOverlayVendor"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:background="@drawable/bg_card_border"
        android:hint="Receiver / Vendor (e.g. Starbucks)"
        android:padding="10dp"
        android:textColor="@color/text_white"
        android:textColorHint="@color/text_muted"
        android:textSize="12sp" />

    <!-- Category Spinner -->
    <Spinner
        android:id="@+id/spOverlayCategory"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/bg_card_border"
        android:padding="8dp" />

    <!-- Note field -->
    <EditText
        android:id="@+id/etOverlayNote"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:background="@drawable/bg_card_border"
        android:hint="Note (optional)"
        android:padding="10dp"
        android:textColor="@color/text_white"
        android:textColorHint="@color/text_muted"
        android:textSize="12sp" />

    <!-- Action Buttons: Dismiss, Snooze, Confirm -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:gravity="end"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btnOverlayDismiss"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:text="@string/dismiss"
            android:textColor="@color/text_muted"
            android:textSize="11sp" />

        <Button
            android:id="@+id/btnOverlaySnooze"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:layout_marginStart="4dp"
            android:text="@string/snooze"
            android:textColor="@color/text_muted"
            android:textSize="11sp" />

        <Button
            android:id="@+id/btnOverlayConfirm"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:layout_marginStart="8dp"
            android:backgroundTint="@color/accent_mint"
            android:text="@string/confirm"
            android:textColor="@color/bg_dark"
            android:textSize="11sp"
            android:textStyle="bold" />
    </LinearLayout>

</LinearLayout>
